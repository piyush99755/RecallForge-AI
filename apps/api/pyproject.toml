[project]
name = "recallforge-api"
version = "0.1.0"
description = "FastAPI backend for RecallForge AI"
requires-python = ">=3.12"

dependencies = [
    "fastapi",
    "uvicorn[standard]",
]

[tool.uv]
dev-dependencies = []