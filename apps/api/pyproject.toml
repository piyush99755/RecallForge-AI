[project]
name = "recallforge-api"
version = "0.1.0"
description = "FastAPI backend for RecallForge AI"
requires-python = ">=3.12"

dependencies = [
     "fastapi",
    "uvicorn[standard]",
    "pydantic-settings",
    "sqlalchemy",
    "psycopg[binary]",
    "alembic",
    "pgvector",
    "python-multipart",
    "pymupdf",
    "google-genai",
]


[tool.uv]
dev-dependencies = []